# YOLO-Seg 與 YOLO-Pose 模型對比實驗報告

## 1. 實驗背景與目的
本實驗旨在客觀評估與對比 **YOLO-Seg (語意分割)** 模型 (`result/train_seg/run6/weights/best.pt`) 與新一代 **YOLO-Pose (17 關鍵點姿勢估計)** 模型 (`result/train_pose_17kpt_merged/run-10/weights/best.pt`) 在 IDPA 靶紙定位任務上的表現。

評估的資料集包含：
*   **`dataset_v1`**：包含 600 張圖片，標註格式為靶紙的 **4 個頂點 (Corners)**。主要用來評估全域邊界框的交疊率 (IoU) 與四個角落的平均絕對誤差 (MAE)。
*   **`0604.yolo26`**：包含 67 張圖片（評估 41 張），標註格式為 **17 個關鍵點** (其中前 12 點為外框點)。主要用來評估複雜形變下的細部特徵點定位能力。

---

## 2. 實驗結果數據 (Quantitative Results)

### 2.1 在 `dataset_v1` 上的表現對比
該測試評估模型能否精確抓出靶紙的四個角落。
*   **Seg 模型處理方式**：提取分割遮罩輪廓 -> HoughLinesP 直線擬合 -> 計算四邊交點提取 4 頂點。
*   **Pose 模型處理方式**：直接輸出 17 點 -> 取外框 12 點做 Homography 對齊標準模板 -> 投射出 4 個標準頂點。

| 評估指標 | YOLO-Seg (`run6`) | YOLO-Pose (`run-10`) | 表現提升 / 差異 |
| :--- | :--- | :--- | :--- |
| **總測試圖片** | 600 | 600 | - |
| **有效預測數量** | 576 *(有 24 張無法成功擬合出四邊形)* | **600** *(100% 成功提取)* | 穩定度提升，無依賴複雜的 OpenCV 擬合 |
| **Average IoU** | 0.6359 | **0.9251** | **大幅提升 45.4%** |
| **Corner MAE (像素誤差)** | 521.19 px | **50.99 px** | **誤差減少 90%** (從 521 像素降至 50 像素) |

> [!TIP]
> **分析**：YOLO-Seg 在面對背景干擾或靶紙部分殘缺時，遮罩輪廓容易扭曲，導致後續的直線擬合與交點計算產生巨大誤差（MAE 爆增至 500+ 像素）。而 YOLO-Pose 直接受惠於 17 點的全局注意力機制，直接輸出精準座標，IoU 高達 0.925，抗遮擋能力遠勝分割演算法。

### 2.2 在 `0604.yolo26` 上的表現對比 (12-Point 評估)
該測試要求精準找出靶紙的 12 個特徵點 (肩部、腰部、切角等)。

| 評估指標 | YOLO-Seg (`run6`) | YOLO-Pose (`run-10`) |
| :--- | :--- | :--- |
| **總評估圖片** | 無法直接評估 | 41 |
| **Valid Predictions** | N/A | 40 |
| **Average 12-Point MAE**| N/A | **86.42 px** |
| **Accuracy (PCK@0.1)** | N/A | **97.71%** |

> [!NOTE]
> **為何沒有 Seg 模型的數據？**
> `YOLO-Seg` 輸出的結果是「連續的像素邊界遮罩 (Mask)」。要在沒有幾何規則的情況下，將一坨不規則的遮罩多邊形「強制映射」回 IDPA 靶紙定義的 12 個精準解剖學/幾何學關鍵點（例如：左肩內側、右腰部折角等）在演算法上是極度困難且不穩定的。
> 
> 這正是本專案**從 Seg 典範轉移至 Pose 典範的根本原因**：Pose 模型天生就能輸出帶有語意順序的關鍵點，不僅能做多目標追蹤，更能直接計算 12 點 MAE，準確率 (PCK@0.1) 高達 97.71%。

---

## 3. 綜合結論 (Conclusion)

1.  **穩定性全面勝出**：YOLO-Pose 在 600 張測試集中達到了 100% 的有效預測率，反觀 YOLO-Seg 因為依賴 OpenCV 的幾何特徵工程（直線擬合、找交點），容易在邊緣破損時失敗。
2.  **誤差大幅收斂**：在還原 4 個角落座標的任務上，YOLO-Pose 將平均絕對誤差 (MAE) 從 521 像素暴降至 50 像素。這對於後續的 Homography + TPS 變形校正極為關鍵，直接決定了計分靶紙有沒有變形失真。
3.  **解決語意匹配痛點**：YOLO-Seg 無法有效應對 12 點以上的複雜多邊形語意點對齊任務。YOLO-Pose 則漂亮解決此問題，在嚴格的 12 點評估中展現 97.71% 的 PCK 精準度。

**最終建議**：未來的開發與部署應全面棄用 `main.py` (Seg 流程)，將所有資源投入在 `main_pose.py` (Pose 流程) 的最佳化與邊緣運算部署。
