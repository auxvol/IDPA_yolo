# IDPA 靶紙偵測系統 - YOLO-Pose 核心技術與演算法說明書

本文件專門彙整並解析 **IDPA 靶紙偵測與追蹤系統** 中，關於 **YOLO-Pose 管線** 所使用的核心電腦視覺技術、深度學習模型以及進階資料增強演算法。

---

## 1. 核心深度學習架構

### 1.1 YOLOv11-Pose (17 關鍵點姿勢估計)
*   **技術說明**：放棄傳統基於 Mask 分割 (Segmentation) 後再用 OpenCV 提取輪廓與多邊形擬合的傳統作法。改用 YOLOv11-Pose 模型，**端到端直接預測 17 個靶紙特徵點**。
*   **技術優勢**：
    *   **抗遮擋能力強**：當靶紙被部分遮擋或邊緣被子彈打破時，傳統邊緣檢測會失效或嚴重扭曲，而 Pose 模型能憑藉全局特徵推斷出隱藏點的精確位置。
    *   **效能提升**：省去了 CPU 端繁重的輪廓找尋 (`findContours`) 與霍夫直線擬合 (`HoughLinesP`) 運算。

---

## 2. 幾何變形與校正管線 (Geometry Pipeline)

為了將傾斜且可能受損變形的靶紙還原成標準計分正視圖，系統使用了 OpenCV 的雙重變形演算法：

### 2.1 全局透視校正 (Homography)
*   **使用函數**：`cv2.findHomography`, `cv2.warpPerspective`
*   **運作原理**：提取 YOLO-Pose 預測的 17 點中外圍的 **12 個邊框點**，與標準靶紙的 12 個正規化座標進行映射。計算出 $3 \times 3$ 的透視變換矩陣 $M$，消除攝影機側拍帶來的 3D 傾斜與透視縮放。

### 2.2 局部非線性精修：薄板樣條 (Thin Plate Spline, TPS)
*   **使用函數**：`cv2.createThinPlateSplineShapeTransformer`
*   **技術痛點解決**：紙本靶紙受到子彈衝擊或風吹時，會產生「物理凹陷」或「波浪狀彎曲」，這是純線性矩陣 (Homography) 無法解決的非剛性形變。
*   **運作原理**：在 Homography 第一次校正的基礎上，將**全部 17 個關鍵點** (包含靶紙內部的 5 個參考點) 餵入 TPS 演算法。TPS 演算法會像「釘釘子」一樣將這 17 個點釘在標準位置，並透過最小化彎曲能量函數，對剩餘的像素進行平滑的非線性插值拉伸，實現極致的幾何對齊。

---

## 3. 穩定性與防呆機制 (Tracking & Fallback)

為了在連續影片中提供穩定的儀表板畫面，避免單幀預測失誤導致畫面閃爍，系統實作了自訂的邏輯狀態機：

### 3.1 兩階段質心追蹤演算法
*   **Phase 1 (校準期)**：系統在最初 3 秒內不會盲目追蹤，而是利用 `cv2.contourArea` 收集外框 12 點的面積陣列。透過取**中位數**，過濾掉初期 YOLO 預測不穩定的極端值，並將最接近中位數的 17 點座標定為 **Baseline (基準)**。
*   **Phase 2 (主動追蹤)**：透過歐氏距離 (`math.hypot`) 匹配上一幀的目標質心，進行多目標獨立追蹤。

### 3.2 面積防呆與 Alpha 空洞防護
*   **5% 面積下降容忍**：現實中靶紙被擊中邊緣後，面積確實會縮小。系統允許面積微幅下降 (2%~5%)，但拒絕瞬間的大幅縮減，防止模型將背景誤認為邊框。
*   **Alpha Channel 回溯機制**：在進行 TPS 變形後，若 YOLO 預測的點嚴重內縮，會導致變形後邊緣出現「透明空洞 (Alpha = 0)」。系統藉由計算 Alpha 空洞數量與點位投影偏差 (Cost)，一旦超標便立即觸發防護，畫面會自動 **回溯 (Rollback)** 使用上一幀的穩定 Baseline，確保視覺輸出永不破圖。

---

## 4. 特化資料增強 (Data Augmentation)

為了讓 YOLO 學習到應對極端狀況的能力，訓練腳本 (`train_yolo_pose.py`) 運用了深度的 Python Monkey-Patch 技術修改底層：

### 4.1 長寬比獨立縮放 (Aspect Ratio Jitter)
*   **技術實作**：覆寫 YOLO 原生 `RandomPerspective` 類別的仿射變換矩陣生成邏輯。
*   **效果**：打破原本單一 `scale` 參數只能等比例縮放的限制，引入 $X$ 軸與 $Y$ 軸的不對稱縮放。強制模型看過「被極度拉長」或「被壓扁」的靶紙，增強對特殊攝影角度的魯棒性。

### 4.2 非線性彈性形變 (Elastic Transform)
*   **使用套件**：`Albumentations (A.ElasticTransform)`
*   **效果**：利用隨機位移場對影像進行像水波紋一樣的扭曲，完美模擬紙張的皺褶與凹折。

### 4.3 Keypoints 座標正規化修補 (Monkey-Patching Albumentations)
*   **技術實作**：覆寫了 `YoloAlbumentations.__call__`。
*   **解決問題**：原生 YOLO 在整合 Albumentations 時，對關鍵點 (Keypoints) 的處理存在致命 Bug（傳入 0~1 的座標，但 Albumentations 需要絕對像素座標）。若不修補，在做上述複雜變形時，17 個關鍵點會被拋棄或錯位。經過手動介入座標系的轉換，確保了所有的形變都能精準且同步地作用在影像與 17 個標籤點上。
