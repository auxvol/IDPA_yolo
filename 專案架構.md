# IDPA 靶紙偵測與追蹤系統 - YOLO-Pose 專案架構說明書

本文件旨在詳細剖析 **IDPA 靶紙偵測與追蹤系統** 中基於 **YOLO-Pose (17 關鍵點)** 的核心架構、資料處理流程、模型訓練策略及幾何校正管線。

---

## 1. 專案簡介與核心特色

本專案將標準 IDPA 靶紙的偵測從傳統的「YOLO-Seg 語意分割 + 直線擬合」升級為 **「YOLO-Pose 17 關鍵點直接預測」**。透過端到端的姿勢估計，系統能更穩定地應對靶紙傾斜、遮擋或邊緣破損的情境，並結合雙重幾何校正技術，還原靶紙正面視角以供後續計分。

### 核心特色
*   **17 點直接估計**：跳過邊緣提取與輪廓簡化，YOLO11-Pose 直接預測靶紙外框 12 點與內部 5 點，大幅減少傳統影像處理造成的抖動誤差。
*   **多目標兩階段追蹤**：獨創 **Phase 1 (校準期)** 與 **Phase 2 (主動追蹤期)** 機制，動態鎖定各靶紙的基準形狀。
*   **防震盪回溯防護**：內建 Alpha 透明空洞檢測機制與 2~5% 面積防呆閾值，自動排除 YOLO 預測異常的幀。
*   **Homography + TPS 雙重校正**：先以外框 12 點進行 Homography 全局透視對齊，再利用全部 17 點進行 TPS (Thin Plate Spline) 薄板樣條局部非線性精修。
*   **高階資料增強訓練**：透過 Monkey-patch 深度修正 YOLO 原生與 Albumentations 的缺陷，完美支援 Keypoints 的長寬比 (Aspect Ratio) 變化與彈性形變 (Elastic Transform)。

---

## 2. 專案目錄結構 (YOLO-Pose 相關)

```text
IDPA_yolo/
├── config/
│   ├── .env                       # 本地環境變數設定
│   └── config.py                  # 全域路徑管理與設定模組
├── data/
│   ├── transfer_pose_dataset.py   # YOLO-Pose (4 點版) 資料轉換
│   ├── transfer_solo_to_yolo17.py # Unity SOLO 至 YOLO-Pose (17 點) 轉換腳本
│   ├── draw_yolo_pose.py          # 驗證 YOLO-Pose 標註視覺化
│   └── unity_data/                # Unity Perception 合成原始資料集
├── src/
│   ├── main_pose.py               # 🏆 YOLO-Pose 多目標追蹤與校正主程式
│   ├── training/
│   │   └── train_yolo_pose.py     # 🏋️ YOLO-Pose 訓練腳本 (含增強修正)
│   └── tests/
│       └── test_video_pipeline_pose.py # 姿勢估計管線影片測試腳本
├── result/                        # 模型權重與訓練評估結果輸出目錄
└── README.md                      # 專案總覽說明
```

---

## 3. 整體系統架構與資料流

```mermaid
graph TD
    %% 影片與模型推論
    Input[輸入實彈射擊影片] --> YOLO[YOLO11-Pose 模型預測]
    YOLO --> Extract[提取 N 個目標的 17 個關鍵點]
    Extract --> AreaCalc[計算外框 12 點多邊形面積與質心]

    %% 狀態機
    AreaCalc --> Tracking{當前追蹤階段?}
    
    Tracking -->|Phase 1: 校準期 (前 3 秒)| Calibration[收集 17 點歷史、面積與質心]
    Calibration --> EndCalib[計算中位數面積，鎖定最優 Baseline]
    
    Tracking -->|Phase 2: 主動追蹤期| ActiveTracking[質心距離匹配追蹤]
    ActiveTracking --> QualityCheck{面積下降 & 防呆驗證?}
    QualityCheck -->|符合閾值| UpdateBaseline[暫存為新候選 Baseline]
    QualityCheck -->|不符| KeepBaseline[維持歷史最佳 Baseline]

    %% 變形與校正管線
    UpdateBaseline --> Pipeline
    KeepBaseline --> Pipeline
    EndCalib --> Pipeline
    
    subgraph 幾何校正管線 (Geometry Pipeline)
        Pipeline[取得最佳 17 關鍵點] --> Homography[12 點 Homography 全局透視對齊]
        Homography --> WarpM[cv2.warpPerspective 產生對齊影像]
        WarpM --> AlphaCheck[Alpha 空洞檢測 / 投影偏差評估]
        
        AlphaCheck -->|品質不佳| Rollback[回溯至前次穩定 Baseline]
        AlphaCheck -->|品質通過| TPS[將 17 點做 TPS 薄板樣條變形精修]
    end

    %% 輸出
    TPS --> Output[輸出校正後靶紙]
    Rollback --> Output
    Output --> Dashboard[Grid 動態儀表板組合顯示]

    classDef core fill:#003366,stroke:#fff,stroke-width:2px,color:#fff;
    classDef model fill:#006600,stroke:#fff,stroke-width:2px,color:#fff;
    classDef pipeline fill:#660000,stroke:#fff,stroke-width:2px,color:#fff;
    
    class YOLO model;
    class Pipeline,Homography,WarpM,TPS pipeline;
    class Calibration,ActiveTracking,QualityCheck core;
```

---

## 4. 核心模組詳解

### 4.1 姿勢估計與多目標追蹤 (`main_pose.py`)

本程式為系統的運行核心，實作了 `TargetTracker` 類別，負責管理每一張靶紙的生命週期。

#### 兩階段追蹤機制：
1.  **Phase 1 (校準收集期)**：影片前 3 秒內，持續收集偵測到的 17 個關鍵點、面積與質心。時間到達後，計算歷史面積的**中位數**，選出最接近中位數的那一幀作為該靶紙的「全局最佳 Baseline (`global_optimal_kpts`)」。此舉能有效排除初期的異常形變。
2.  **Phase 2 (主動追蹤期)**：利用質心距離 (Hypot) 匹配先前的目標。若新預測的面積比最佳面積更小，且降幅在 **5% 內**，系統將暫存此幀為新候選（因為子彈可能削去邊緣，但不可能瞬間大幅縮小）。

#### Homography + TPS 幾何管線：
1.  從 17 點中提取 `OUTLINE_INDICES` (外圍 12 點)。
2.  透過 `cv2.findHomography` 將外圍 12 點對齊至「標準 IDPA 靶紙正規化座標」。
3.  利用 `cv2.warpPerspective` 進行第一次 3D 透視校正。
4.  利用透視矩陣將 17 個點投影到對齊後的空間。
5.  由於紙張受彈擊可能有物理凹陷與波浪形變，Homography (線性透視) 無法處理。系統接著呼叫 `cv2.createThinPlateSplineShapeTransformer`，利用**全部 17 個點**進行非線性的 **TPS 薄板樣條** 精修。
6.  **Alpha 空洞回溯防護**：TPS 變形後，系統會檢查影像的 Alpha Channel。如果發現非預期的透明空洞 (表示 YOLO 的預測邊緣向內收縮過多導致穿幫)，或 12 點的投影偏差 Cost > 3000，會觸發回溯機制，退回上一個安全的 Baseline。

### 4.2 YOLO-Pose 增強訓練特化 (`train_yolo_pose.py`)

為了讓 YOLO 學習靶紙的各類視角變化，訓練腳本進行了深度的客製化與底層修補 (Monkey Patch)：

1.  **Aspect Ratio 長寬比修補**：
    *   原生 YOLO 的 `RandomPerspective` 缺乏獨立的 X/Y 軸縮放比例變化。
    *   腳本覆寫了 `_compute_affine_matrix`，注入了 `aspect_ratio_jitter = random.uniform(0.8, 1.4)`，讓訓練資料能產生拉長的傾斜靶紙，顯著增強對極端視角的魯棒性。
2.  **Albumentations Keypoints 正規化修復**：
    *   YOLO 整合 Albumentations 時，對 Keypoints (姿態點) 的座標轉換存在 Bug（ Albumentations 期望絕對像素，YOLO 傳入 0~1 的正規化座標）。
    *   腳本攔截並覆寫了 `YoloAlbumentations.__call__`，在送入 A.Compose 前先乘上圖片長寬轉為絕對座標，變形後再除以長寬還原，確保 17 個點不會因為資料增強而丟失或錯位。
3.  **ElasticTransform 非線性增強**：
    *   為模擬靶紙遭受風吹或子彈衝擊的皺褶，引入 `A.ElasticTransform(alpha=1500, sigma=30, p=0.4)`，進行非剛性的局部彈性形變。

### 4.3 Unity SOLO 資料集轉換 (`transfer_solo_to_yolo17.py`)

本腳本負責將 Unity Perception 生成的合成資料 (SOLO 格式) 轉為 Ultralytics YOLO 的格式。

*   **格式要求**：`kpt_shape: [17, 3]` (X, Y, 可見度狀態)
*   **狀態映射**：
    *   Unity `state=1` (Occluded) -> YOLO `v=1`
    *   Unity `state=2` (Visible) -> YOLO `v=2`
*   **BBox 動態計算**：依據 17 個有效點的最小/最大邊界，外擴 5% 自動生成 Bounding Box，確保能完美包覆目標。
*   **隨機切分**：固定亂數種子，將資料集以 80% (Train) / 20% (Val) 切分，並生成對應的 `data.yaml`。

---

## 5. 開發與執行指引

### 環境依賴
專案採用 `uv` 進行套件管理。環境配置包含：
*   `ultralytics >= 8.4`
*   `opencv-contrib-python` (用於 TPS 變形演算法)
*   `albumentations` (進階資料增強)

### 快速執行指令

```bash
# 1. 執行 YOLO-Pose 多目標追蹤管線
python src/main_pose.py

# 2. 啟動 YOLO-Pose 模型訓練 (17 點)
python src/training/train_yolo_pose.py

# 3. 資料集轉換
python data/transfer_solo_to_yolo17.py
```

### 路徑規範
全專案嚴禁使用硬編碼絕對路徑。需透過 `config/config.py` 中的 `settings.get_path()` 動態取得，如：
```python
from config.config import settings
model_path = settings.get_path("result", "train_pose_17kpt_merged", "run-10", "weights", "best.pt")
```
